## Files changed by 134d4e49..dc462d90
M	docs/fork-surface.md
## Remote tags on GitHub pointing at a460e747, 03961847 or 74e1a957 (none expected: no tag published)
none
## Remote tag names mentioning rebase/wholesale
none
## GitHub branch record for fm/fm-wholesale-creator-rebase-w1 (protection unchanged)
{"name":"fm/fm-wholesale-creator-rebase-w1","protected":false,"sha":"a460e74715ca8b1368ed863595e1d4e28e4fe7ea"}
## PR #160 merge record
{"base_ref":"main","head_ref":"fm/fm-wholesale-creator-rebase-w1","head_sha":"a460e74715ca8b1368ed863595e1d4e28e4fe7ea","merge_commit_sha":"03961847c35b6a56c6a3dd8cf99222ab18cbf156","merged_at":"2026-09-17T19:01:55Z"}
## PR #160 body lines mentioning the rollback anchor / merge-commit request
5:The re-apply covers: the ~193 fork-only files from the pre-wholesale Amplify-Logic main (74e1a957); the guarded fork call sites in the six extracted files; the remaining fork customisations in heavy shared files, especially bin/fm-spawn.sh and bin/fm-test-run.sh; and our own AGENTS.md and fork instruction surface, which stays fork-owned. fork-surface.conf / bin/fm-fork-surface.sh and the fork test registry are the proof that our declared capabilities still hold.
9:- Landing must be a merge commit, not a squash, so rollback is a revert of the merge commit and the history of the re-base survives.
10:- The PR body must carry a Rollback section naming: the pre-wholesale tip 74e1a957 as the rollback anchor, with its full commit URL on Amplify-Logic/firstmate; that live firstmate homes must NOT run /updatefirstmate until the captain accepts the soak; and that recovery is reverting the merge commit and leaving homes on 74e1a957.
24:- The branch tip is now the creator's firstmate tree (`kunchenguid/firstmate` at `b85e28b5`) with this fork's customisations re-applied on top, instead of upstream commits cherry-picked into the old Amplify-Logic `main`. That covers the ~193 fork-only files from the pre-wholesale tip `74e1a957`, the guarded fork call sites in the six extracted files, the remaining fork edits in the heavy shared files (notably `bin/fm-spawn.sh` and `bin/fm-test-run.sh`), and the fork-owned `AGENTS.md` and instruction surface. `fork-surface.conf` / `bin/fm-fork-surface.sh` and `tests/fork-test-registry.conf` re-declare the fork's 70 capabilities against the new upstream base as the anti-regression proof.
28:## Rollback
30:- Rollback anchor is the pre-wholesale tip `74e1a957`: https://github.com/Amplify-Logic/firstmate/commit/74e1a9574bf7cffc6cb8912e746d330805a3b765
31:- Land this as a **merge commit, not a squash**, so rollback is a revert of the merge commit and the history of the re-base survives. This PR is not to be merged by the pipeline or by an agent — the captain merges it himself after he accepts a soak period.
33:- Recovery: revert the merge commit and leave homes on `74e1a957`.
45:<code>I derived the scenarios from the last round&#39;s two deliverables (a non-vacuous herdr presentation fixture and the fork-surface declaration for the extracted push-transition lib) plus the presentation behaviour the preceding commits restored, and drove each against the real scripts with fake herdr endpoints. Baseline gates first: fork-surface check, the doc-audience gate and the coverage guard are all green. The product surface here is the operator-facing pane label, so the evidence is the actual herdr command transcripts: a completed teardown clears the retired pane&#39;s tokens, renames its tab back to fm-task-x1 and re-projects a surviving task&#39;s pane as &#34;WORKER · task x2 · WAITING&#34;, while a refused teardown of a live task emits no --clear-token and no tab rename at all. Reverting the clear-placement fix makes that retain case fail with the exact pane-clear command a live worker would have received, so the guard is a real regression test. The watcher&#39;s push transition projects &#34;WORKER · tk2 · WAITING&#34; and its test goes red when the restored call site is deleted. Both adversarial mutations of the presentation fixture trip the new guard, and with the guard stripped the same drift lets the retain case pass while proving nothing — which is exactly what it was added to stop. Two honest caveats. The fork-surface anchor added this round cannot fail today: G5 skips anchors for personal-scope capabilities, verified by deleting the call site (gate stayed green) and by flipping only the scope to team (gate failed correctly); I reverted both and left the tool alone per the captain&#39;s parked decision. And tests/fm-teardown.test.sh has one failing case on this host, herdr-preflight-missing-adapter, where stock macOS Bash 3.2 terminates the script at the missing `. backends/herdr.sh` before the refusal branch can run, so teardown exits 0 silently without touching records, branch or worktree; the identical failure reproduces from a pristine checkout of the creator base b85e28b5, and no Bash &gt;= 4 or container runtime exists here, so that one case is reported untested rather than failed. The other 85 tests in that suite are green. No screenshots apply — this change has no rendered UI; the reviewer-visible surface is the multiplexer command stream, captured verbatim.</code>
52:| A herdr teardown that refuses and keeps a live task leaves that task&#39;s pane label exactly as it found it | ✅ pass | live | `bash tests/fm-teardown.test.sh` case herdr-retain-presentation; herdr-pane-commands-retained.log contains no --clear-token and no `tab rename ... fm-task-x1`. Reverting 40218c84&#39;s placement makes the… |
155:reverted placement
215:Both mutations were reverted; the worktree is clean and the gate is green.
269:fork's pre-wholesale tip 74e1a957; both arrive with the creator tree. The fork's
354:ok - a legacy stamp a failed rollback left behind still faces the endpoint gate
412:ok - event_wait_or_sleep: consecutive event-path failures disable the fast-path and revert to pure polling (fail-closed)
497:- ⚠️ `bin/fm-teardown.sh:3511` - The restored `--clear &#34;$ID&#34;` is placed before gates that deliberately retain a LIVE task and exit, so it strips presentation from a pane whose task is not retired. Concrete sequence on a protocol&gt;=16 herdr build: forced teardown of a herdr task reaches 3511 and clears the pane&#39;s fm_task_id/fm_runtime/fm_state tokens plus renames the tab back to `fm-&lt;id&gt;`; then `teardown_herdr_session_lock_held` is false (or the pane presence is unparseable), so the close is skipped with a warning and `fm_backend_herdr_endpoint_confirmed_gone` at 3560 fails, and teardown exits 1 with &#34;retaining every durable task record - rerun teardown once the close can run under the session lock&#34;. The agent in that pane was never killed, so the operator is left with a running worker whose pane carries no label at all, and nothing re-projects it: the watcher `--all` only fires on a signal batch, and a stuck worker produces none. tests/fm-teardown.test.sh:2187 (`test_herdr_flat_teardown_refuses_records_on_unparseable_presence`) already drives exactly this retain path; it does not catch the regression only because its fake herdr reports no protocol, so `fm_backend_herdr_presentation_capable` returns 1 and the clear is a no-op in the fixture. Note the inverse too: past the 3560 gate the recorded pane is proven `dead` (bin/backends/herdr.sh:3497-3502), so on a teardown that actually completes, the pre-close pane clear cannot be the thing that saves a surviving pane. The smallest correct placement under the creator&#39;s current control flow (which the user instruction asks for, rather than the old line number) is after the endpoint-confirmed-gone gate and before the record removal, where the tab retirement still lands and no live task can lose its label. Also narrow the call: `74e1a957:bin/fm-teardown.sh:1450` guarded it with `[ &#34;$BACKEND&#34; != orca ]`, while the restored call is unconditional, so every tmux/zellij/orca/cmux teardown now pays a `herdr status --json` + `jq` capability probe for a `clear_task` that returns at its own `backend=herdr` check.
503:- ⚠️ `fork-surface.conf:82` - Round 13 added a fork-only call site to `bin/fm-push-transition-lib.sh:153` (`fm-visible-status.sh &#34;$task&#34;` on a push transition), but that file appears nowhere in fork-surface.conf or tests/fork-test-registry.conf — `grep -n fm-push-transition-lib` over both returns nothing. At the pre-wholesale tip this code lived in `74e1a957:bin/fm-watch.sh:948`, and `bin/fm-watch.sh` IS declared under `herdr-worker-presentation`&#39;s `modifies` (line 82); the creator extracted the function into a new lib and the declaration did not follow it. The intent makes fork-surface.conf &#34;the proof that our declared capabilities still hold&#34;, so a fork customisation in an undeclared creator file is exactly the state that lets the next wholesale re-base drop it with no record that it existed. I am not proposing the fix here: the captain already approved option C for the fork-surface work in its own round with a full budget, and this belongs in that round alongside the four existing declaration findings rather than as a one-line patch now. Naming it so it is not lost.
515:- ℹ️ `fork-surface.conf:89` - The fork-surface anchor added in 23df7969 for bin/fm-push-transition-lib.sh is a record, not a guard: I deleted the restored call site at bin/fm-push-transition-lib.sh:153 and `bin/fm-fork-surface.sh check` still reported FORK_SURFACE OK, because G5 skips anchors whenever a capability&#39;s scope is `personal`, which herdr-worker-presentation is (this also makes its two pre-existing anchors inert). The declared pattern itself is correct: with the same call site deleted and only `scope` flipped to `team`, the gate fails with &#34;anchor missing in bin/fm-push-transition-lib.sh&#34;. Both mutations were reverted and the gate is green. Reported only; the captain has already parked fork-surface strengthening as its own report-only round, and the call site does have real behavioural cover (tests/fm-supervision-events.test.sh fails when it is removed).
521:| A herdr teardown that refuses and keeps a live task leaves that task&#39;s pane label exactly as it found it | ✅ pass | live | `bash tests/fm-teardown.test.sh` case herdr-retain-presentation; herdr-pane-commands-retained.log contains no --clear-token and no `tab rename ... fm-task-x1`. Reverting 40218c84&#39;s placement makes the… |
