projects/
state/
data/
.no-mistakes/
.lavish/
.fm-secondmate-home
.DS_Store
__pycache__/
*.pyc
.env
# Captain-private local operating choices; never published. Anchored to the
# repo root so a config-named directory elsewhere in the tree stays visible.
/config/
# Generated per-task hook artifacts (also written into each task worktree's
# .git/info/exclude by fm-spawn). Tracked here so a primary checkout cannot
# accidentally stage them into a PR.
.claude/settings.local.json
.cursor/hooks.json
.opencode/plugins/fm-turn-end.js
.fm-grok-turnend
.fm-lint-parity.*
